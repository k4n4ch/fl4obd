# FL4 OBD MONITOR — 設計仕様書

## 目的

Honda FL4 (Civic e:HEV) の OBD-II データをスマートフォンのブラウザでリアルタイム表示する。

---

## 方針

- サーバー不要。単一 HTML ファイルで完結。
- Web Bluetooth API 経由で ELM327 BLE ドングルに接続。
- Android Chrome（または HTTPS 環境）で動作。

---

## 技術スタック

| 項目 | 内容 |
|------|------|
| 実装 | HTML / CSS / JavaScript（バニラ） |
| 通信 | Web Bluetooth API → ELM327 BLE ドングル → OBD-II (ISO 15765-2 CAN) |
| ストレージ | なし（揮発性） |

---

## 表示項目

| 表示 | PID | TA | 変換式 | 単位 |
|------|-----|----|--------|------|
| ENGINE RPM | 0x0C | 01 | (256A+B)/4 | rpm |
| SPEED | 0x0D | 01 | A | km/h |
| VBAT / CURRENT / BATTERY PWR | 0x9A | 01 | V=(256C+D)/64, I=signed16(256E+F)×0.1, P=V×I/1000 | V / A / kW |
| STATE OF CHARGE | 0x5B | 01 | 100A/255 | % |
| SYS V | 0x42 | 01 | (256A+B)/1000 | V |
| RUNTIME | 0x1F | 01 | 256A+B | sec |
| ENG TEMP（オプション） | 0x67 | 10 | B−40 (sensor1) | ℃ |
| CAT TEMP（オプション） | 0x3C | 10 | (256A+B)/10−40 | ℃ |
| INT TEMP（オプション） | 0x68 | 10 | B−40 (sensor1) | ℃ |

---

## CAN 通信設定

```
TA_BATT  : 18DAF101  (常時ポーリング対象)

ELM327 初期化シーケンス:
  ATE0  — エコーオフ
  ATL0  — 改行なし
  ATS0  — スペースなし
  ATSP0 — プロトコル自動
  ATSH18DAF101   — デフォルトヘッダー (TA_BATT)
  ATFCSH18DAF101 — フローコントロールヘッダー
  ATFCSD300000   — フローコントロールデータ
  ATFCSM1        — フローコントロールモード1

起動時のみ:
  011F (TA_BATT のまま取得)
```

---

## ポーリング方式

レスポンス駆動（`>` 受信時に次PIDを即送信）+ ウォッチドッグ200ms。

```
通常キュー:
  010C → 019A → 010D → 015B → 0142 → (繰り返し)

温度トグル ON 時:
  010C → 019A → 010D → 015B → 0142
  → ATSH18DAF110 → 0167 → 013C → 0168
  → ATSH18DAF101 → ATFCSH18DAF101 → (繰り返し)
```

AT コマンドも `>` を返すためレスポンス駆動ループをそのまま利用できる。温度 ON 時は1サイクルに ATSH×2 の往復が増える分ポーリングレートが下がるが許容範囲。

---

## PID 0x9A (マルチフレーム) パース仕様

ELM327 独自フォーマットで返る。

```
"008 0:419A07004245 1:000D55555555"
       ├─ FF (First Frame)  : 0x41 0x9A [A][B][C][D]
       └─ CF (Consecutive)  : [E][F][pad...]

VBAT [V]    = (256×C + D) / 64
IBAT [A]    = signed16(256×E + F) × 0.1
PBAT [kW]   = VBAT × IBAT / 1000
```

FFとCFが `>` をまたいで別ブロックに分かれることがある。`pendingFF` 変数で前ブロックのFFを保持して次ブロックのCFと結合する。

---

## BLE サービス UUID

| Type | Service UUID |
|------|-------------|
| A (一般的な中華ドングル) | `0000fff0-0000-1000-8000-00805f9b34fb` |
| B (iOS向けドングル) | `e7810a71-73ae-499d-8c15-faa9aef0c3f2` |

両方を順に試し、Notify/Write キャラクタリスティックを自動検出する。

---

## 画面構成

```
┌─────────────────────────────────────┐
│  2×2 アナログメーター               │
│  [ENGINE RPM] [SPEED]               │
│  [BATTERY PWR][STATE OF CHARGE]     │
├─────────────────────────────────────┤
│  VBAT | CURRENT | SYS V | RUNTIME   │
├─────────────────────────────────────┤
│  [ TEMP ]  ← OFF時はボタン          │
│  ENG | CAT | INT  ← ON時は温度表示  │
│  (タップで ON/OFF 切替)             │
├─────────────────────────────────────┤
│  FL4 OBD MONITOR  [⟳横] [STATUS] ● │
│  [ BLUETOOTH 接続 ]                 │
│  ログエリア                [● REC]  │
│  [ DEMO ]                           │
└─────────────────────────────────────┘
```

---

## 機能

| 機能 | 説明 |
|------|------|
| BLE接続 | ELM327 BLEドングルを名前/サービスUUIDでスキャン・接続 |
| リアルタイム表示 | 200ms周期で描画更新 |
| 温度トグル | TEMP ボタンで ENG/CAT/INT 温度を一括 ON/OFF。ON 時のみ TA=10 をポーリング |
| ログ記録 | REC開始〜停止でタイムスタンプ付きテキストをダウンロード |
| デモモード | 未接続時に模擬データでメーターを動作確認 |
| 横向きモード | メーターカードを90°回転（端末向き変更にも自動追従） |
| スリープ抑制 | Screen Wake Lock API でディスプレイをオンに保持 |

---

## 制約・既知仕様

- **Android Chrome + HTTPS（またはlocalhost）必須**。iOS Safariは Web Bluetooth 非対応。
- PID 0x9A は TA=01 専用。他 ECU では応答なし。
- RUNTIME (0x1F) はエンジン始動後に TA=01 で1回だけ取得。以降は JS タイマーでカウントアップ。

---

## SOC ゲージ色設計

HEV（エンジン併用）の動作域を反映した3ゾーン固定色帯。

| ゾーン | 範囲 | 色 | 意味 |
|--------|------|----|------|
| 枯渇域 | 0〜25% | 赤 | SOC 不足。エンジン始動優先 |
| 正常域 | 25〜85% | シアン | 通常の EV 走行域 |
| 過充電域 | 85〜100% | オレンジ | 回生ブレーキが入り切らない |

背景弧に3色の固定帯を描き、SOC 位置より先をカード背景色で覆うことで点灯/消灯を表現。針（バー）は現在ゾーンの色で表示。
